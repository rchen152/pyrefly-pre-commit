[project]
name = "specific-version-hook-example"
version = "0.1.0"
description = "Example project using pyrefly-typecheck-specific-version hook"
requires-python = ">=3.9"
dependencies = [
    "requests==2.32.4",
]

[project.optional-dependencies]
dev = [
    "pre-commit==4.5.0",
]
