[project]
name = "system-hook-example"
version = "0.1.0"
description = "Example project using pyrefly hook with system install"
requires-python = ">=3.9"
dependencies = [
    "packaging==25.0",
]

[dependency-groups]
dev = [
    "pre-commit==4.5.0",
    "pyrefly==0.42.0",
]
